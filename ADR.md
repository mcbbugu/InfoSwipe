# 架构决策记录 (ADR)

本文档记录 InfoSwipe MVP 开发过程中的关键架构决策。

## ADR-001: 选择 Fastify 作为后端框架

**日期**: 2024-12-19  
**状态**: 已采用

### 决策背景
需要快速构建高性能的 RESTful API 服务。

### 考虑的方案
1. Express.js - 最流行的 Node.js 框架
2. Fastify - 高性能、低开销的框架
3. Koa.js - 基于 async/await 的框架

### 决策结果
选择 Fastify。

### 理由
- **性能优势**：Fastify 比 Express 快 2-3 倍
- **TypeScript 友好**：虽然本项目用 JavaScript，但未来扩展性好
- **插件生态**：丰富的插件系统，如 @fastify/cors
- **内置日志**：无需额外配置日志系统
- **符合挑战要求**：技术栈自选，Fastify 是合理选择

### 后果
- 需要学习 Fastify 的插件注册方式
- 社区资源相对 Express 较少，但足够使用

---

## ADR-002: 使用 SQLite 作为数据库

**日期**: 2024-12-19  
**状态**: 已采用

### 决策背景
需要轻量级、无需额外服务的数据库解决方案。

### 考虑的方案
1. SQLite - 文件数据库
2. PostgreSQL - 关系型数据库
3. MongoDB - NoSQL 数据库

### 决策结果
选择 SQLite。

### 理由
- **零配置**：无需安装和配置数据库服务
- **轻量级**：适合 MVP 阶段
- **一键启动**：符合挑战要求的"一键启动"
- **性能足够**：对于 MVP 规模的数据量完全够用
- **better-sqlite3**：同步 API，代码更简洁

### 后果
- 文件数据库，需要注意备份
- 并发写入性能有限，但 MVP 阶段足够

---

## ADR-003: 使用 Vue 3 + Vite 作为前端框架

**日期**: 2024-12-19  
**状态**: 已采用

### 决策背景
需要快速构建响应式、交互丰富的前端界面。

### 考虑的方案
1. Vue 3 + Vite - 现代 Vue 生态
2. React + Vite - React 生态
3. Svelte - 编译时框架

### 决策结果
选择 Vue 3 + Vite。

### 理由
- **开发体验**：Vue 3 Composition API 代码更清晰
- **性能**：Vite 提供极快的开发体验
- **学习曲线**：相对 React 更平缓
- **生态成熟**：Vue Router、Tailwind CSS 集成简单

### 后果
- Vue 3 的响应式系统需要理解
- 组件化开发需要合理拆分

---

## ADR-004: 使用 Tailwind CSS 作为样式方案

**日期**: 2024-12-19  
**状态**: 已采用

### 决策背景
需要快速构建美观的 UI，避免编写大量 CSS。

### 考虑的方案
1. Tailwind CSS - 工具类优先
2. 传统 CSS - 完全自定义
3. CSS-in-JS - 运行时样式

### 决策结果
选择 Tailwind CSS。

### 理由
- **开发速度**：工具类快速构建 UI
- **一致性**：内置设计系统保证一致性
- **响应式**：内置响应式工具类
- **体积优化**：生产环境自动移除未使用的样式

### 后果
- HTML 类名较多，但可读性好
- 需要配置 PostCSS

---

## ADR-005: 滑动交互实现方案

**日期**: 2024-12-19  
**状态**: 已采用

### 决策背景
核心功能需要实现三向滑动交互（上/左/右）。

### 考虑的方案
1. 原生 touch/mouse 事件 - 手动实现
2. Hammer.js - 手势库
3. Swiper.js - 轮播库

### 决策结果
选择原生事件实现。

### 理由
- **轻量级**：无需额外依赖
- **控制精确**：完全控制交互逻辑
- **性能**：直接操作 DOM，性能最优
- **MVP 阶段**：功能简单，原生实现足够

### 后果
- 需要处理 touch 和 mouse 两套事件
- 需要手动计算滑动距离和方向

---

## ADR-006: 去重算法选择

**日期**: 2024-12-19  
**状态**: 已采用

### 决策背景
需要可解释的去重方法，避免重复文章。

### 考虑的方案
1. 哈希去重 + 相似度算法 - 双重保障
2. 仅哈希去重 - 简单但可能漏检
3. 语义相似度 - 更智能但复杂

### 决策结果
选择哈希去重 + Dice 系数相似度算法。

### 理由
- **可解释性**：相似度百分比清晰明了
- **性能**：先哈希快速过滤，再相似度精确判断
- **阈值可调**：标题 85%，内容 70% 可调整
- **string-similarity**：成熟的相似度库

### 后果
- 需要存储哈希值，增加存储
- 相似度计算有一定性能开销，但可接受

---

## ADR-007: 评分维度设计

**日期**: 2024-12-19  
**状态**: 已采用

### 决策背景
需要 3-5 个可解释、可复现的评分维度。

### 考虑的方案
1. 5 个维度，各 20 分 - 总分 100 分
2. 3 个维度，各 33 分 - 总分 99 分
3. 机器学习模型 - 更智能但不可解释

### 决策结果
选择 5 个维度，各 20 分，总分 100 分。

### 理由
- **可解释性**：每个维度规则清晰
- **可复现**：基于明确规则，结果一致
- **平衡性**：5 个维度覆盖文章质量各个方面
- **易调整**：每个维度独立，便于优化

### 维度设计
1. 标题质量（长度、关键词）
2. 内容长度（适中最好）
3. 来源权威性（来源评分）
4. 时效性（发布时间）
5. 关键词密度（技术相关）

### 后果
- 评分规则需要根据实际效果调整
- 存储评分详情需要额外字段

---

## ADR-008: 数据存储结构

**日期**: 2024-12-19  
**状态**: 已采用

### 决策背景
需要设计数据库表结构存储文章和操作历史。

### 决策结果
两张表：articles（文章）和 operations（操作历史）。

### 理由
- **分离关注点**：文章数据和操作历史分离
- **查询效率**：articles 表有索引，查询快
- **可追溯**：operations 表记录所有操作
- **扩展性**：未来可添加用户、标签等表

### 表结构
- **articles**: id, title, content, link, source, published_at, category, score, score_details, dedupe_hash
- **operations**: id, article_id, action, created_at

### 后果
- 需要 JOIN 查询获取完整信息
- 操作历史会随时间增长，需要考虑清理策略


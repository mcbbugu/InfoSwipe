# InfoSwipe 开发规则

## 代码改动记录规范

### 何时更新 DEVLOG.md

在以下情况下，**必须**更新 `DEVLOG.md`：

1. **重大功能改动**
   - 新增核心功能模块
   - 修改核心业务逻辑
   - 重构主要组件或服务

2. **性能优化**
   - 使用 requestAnimationFrame、虚拟滚动等性能优化
   - 数据库查询优化
   - 前端渲染优化

3. **用户体验改进**
   - 交互方式重大调整（如本次滑动优化）
   - UI/UX 显著改进
   - 新增用户反馈机制

4. **架构调整**
   - 技术栈变更
   - 数据库结构变更
   - API 接口重大变更

5. **问题修复**
   - 修复关键 bug
   - 解决性能问题
   - 修复安全漏洞

### 更新格式

在 DEVLOG.md 中添加新的日期章节，格式如下：

```markdown
## YYYY-MM-DD

### 改动类型（功能/优化/修复）

#### 具体改动
- 改动1：描述
- 改动2：描述

#### 技术细节
- 使用的技术/方法
- 关键代码位置

#### 影响范围
- 影响的模块/组件
- 用户体验变化
```

### 不需要记录的情况

以下情况**不需要**更新 DEVLOG.md：
- 小的 bug 修复（不影响核心功能）
- 代码格式调整
- 注释添加
- 小的样式微调（不影响交互）

## 代码质量规范

1. **提交前检查**
   - 运行 linter 检查
   - 确保无 console.log 残留
   - 确保错误处理完善

2. **性能考虑**
   - 避免不必要的响应式更新
   - 使用 requestAnimationFrame 处理高频更新
   - 合理使用 will-change 提示浏览器优化

3. **用户体验**
   - 所有异步操作都要有加载状态
   - 错误要有友好的提示
   - 操作要有明确的反馈

## ADR 更新规范

在以下情况需要更新 `ADR.md`：
- 引入新的技术栈
- 改变架构决策
- 选择新的解决方案

## 文档同步

- README.md：功能变更时更新
- ADR.md：架构决策变更时更新
- DEVLOG.md：重大改动时更新（按本规则）

